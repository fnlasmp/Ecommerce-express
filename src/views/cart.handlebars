<h1>Cart</h1>

{{#if products}}
  <ul>
    {{#each products}}
      <li>
        {{product.title}} - Quantity: {{quantity}} - Price: ${{product.price}}
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>Your cart is empty.</p>
{{/if}}